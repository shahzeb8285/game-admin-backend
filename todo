
if no parent in agent then make root as parent
1. create the first super admin with root name 


ui

1. change the user bonus to game rebates history in ui
2. in agent list show toggle for enable disable after all agents




3. implement counts in fetch all 




ui

in agent list the divider should be visible on expand 

change the label agent to the parent agent 
on upper header show transfer in and transfer out 

map it to the no of pending txns 
if pending> 0 make it red otherwise no color changes

1. Admin Logs are not properly integrated



2. Data should be in desc by date

3. check pagination on all pages

4. toast in api fail case 









